{
  "name": "libmp3lame.js",
  "version": "1.0.0",
  "description": "LAME bindings for JavaScript",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "nix build -o libmp3lame -L .#lame && cp --no-preserve=mode libmp3lame/lib/* lib/ && rm libmp3lame",
    "example:web": "f() { cd examples/web/$1; trap 'kill $(jobs -p)' EXIT; parcel index.html *.wasm & sleep 1; firefox http://127.0.0.1:1234/index.html & wait; }; f",
    "example:node": "f() { node examples/node/$1.mjs; }; f"
  },
  "author": "leo60228",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "browserslist": [
    "last 1 Firefox version"
  ]
}
